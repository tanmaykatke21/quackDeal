{
  "version": 2,
  "builds": [{ "src": "server.js", "use": "@vercel/node", "config": { "maxDuration": 30 } }],
  "routes": [{ "src": "/(.*)", "dest": "server.js" }],
  "crons": [
    {
      "path": "/api/reminders/scan",
      "schedule": "0 9 * * *"
    }
  ]
}
